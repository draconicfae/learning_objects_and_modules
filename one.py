#!/usr/bin/env python3

#level one : level zero plus the system knows we're python so we don't need to say that anymore
#in other words, we can now do ./one.py (if we make it executable) instead of needing to do
#python3 one.py
print('hello world')
